{
    "title": "Crawl4AI Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "urls": {
            "title": "URLs to crawl",
            "type": "array",
            "description": "List of URLs you want to convert to Markdown. Provide one or more full URLs.",
            "editor": "stringList",
            "prefill": ["https://apify.com"]
        },
        "extraction_strategy": {
            "title": "Extraction Strategy",
            "type": "string",
            "description": "Choose how you want the data to be extracted: as clean Markdown, structured JSON, or plain text.",
            "enum": ["markdown", "json", "text"],
            "default": "markdown"
        }
    },
    "required": ["urls"]
}

