{
    "actorSpecification": 1,
    "title": "Crawl4AI Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "urls": {
            "title": "URLs to crawl",
            "type": "array",
            "description": "Provide URLs starting with https://",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://apify.com" }
            ]
        }
    },
    "required": ["urls"]
}
