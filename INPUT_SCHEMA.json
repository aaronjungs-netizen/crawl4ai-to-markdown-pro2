{
    "title": "Crawl4AI to Markdown Pro",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "url": {
            "title": "URL to crawl",
            "type": "string",
            "description": "Enter the URL you want to convert to markdown",
            "editor": "textfield"
        }
    },
    "required": ["url"]
}
